var stateManager = require('../app/stateManager');

describe('component view drop spots', () => {
  it('', () => {
    expect(true).toEqual(true);
  });
});

describe('tree view drop spots', () => {
  it('', () => {
    expect(true).toEqual(true);
  });
});
